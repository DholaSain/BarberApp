import 'package:flutter/material.dart';

class BAccountView extends StatelessWidget {
  const BAccountView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('BAccountView'),
      ),
    );
  }
}
