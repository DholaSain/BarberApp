import 'package:flutter/material.dart';

class BHomeView extends StatelessWidget {
  const BHomeView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('BHomeView'),
      ),
    );
  }
}
